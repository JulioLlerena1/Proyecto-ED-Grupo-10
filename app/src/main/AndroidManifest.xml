<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pro"
        tools:targetApi="31">
        <activity
            android:name=".AgregarAeropuerto"
            android:exported="false" />
        <activity
            android:name=".BuscarRutas"
            android:exported="false" />
        <activity
            android:name=".ConfiguracionVuelos"
            android:exported="false" />
        <activity
            android:name=".ConfiguracionAeropuertos"
            android:exported="false" />
        <activity
            android:name=".PrimerPantalla"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Otras pantallas -->
        <activity android:name=".MainActivity" />
        <activity android:name=".AeropuertoInfo" />
        <activity android:name=".RutaCorta" />
        <activity android:name=".Estadisticas"/>
     



    </application>

</manifest>